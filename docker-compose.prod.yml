version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres-db_demo
    environment:
      - POSTGRES_USER=emd_user
      - POSTGRES_PASSWORD=emd_password
      - POSTGRES_DB=emd_demo
    ports:
      - "5432:5432"
    volumes:
      - postgres-data_emd:/var/lib/postgresql/data
    networks:
      - app-network
    restart: unless-stopped

  backend:
    image: ${CI_REGISTRY_IMAGE}/backend:latest
    container_name: emd_node-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=emd_user
      - DB_PASSWORD=emd_password
      - DB_NAME=emd_demo
    depends_on:
      - postgres
    networks:
      - app-network
    restart: unless-stopped

  frontend:
    image: ${CI_REGISTRY_IMAGE}/frontend:latest
    container_name: emd_react-frontend
    volumes:
      - ./frontend/build:/app/build
    depends_on:
      - backend
    networks:
      - app-network
    restart: unless-stopped

volumes:
  postgres-data_emd:

networks:
  app-network:
    driver: bridge
